# Legacy Content Inventory (Next.js → Astro migration)

Internal migration record. Not public copy. Captures what existed in the
pre-migration Next.js app (last commit on `main`: `d09a676`) before the
framework was replaced on `claudeRevamp`. Distinguishes durable facts from
presentational implementation that was intentionally discarded.

## Routes (preserved as-is)

| Route | Old file | New file | Notes |
|---|---|---|---|
| `/` | `app/page.tsx` | `src/pages/index.astro` | Home |
| `/expertise` | `app/expertise/page.tsx` | `src/pages/expertise.astro` | Practice areas |
| `/about` | `app/about/page.tsx` | `src/pages/about.astro` | Russ bio + credentials |
| `/contact` | `app/contact-us/page.tsx` | `src/pages/contact.astro` | Moved to `/contact` per explicit decision on 2026-07-18; `/contact-us` now 301s to `/contact` via `netlify.toml` (no duplicate indexable page). |
| `/payment` | `app/payment/page.tsx` | `src/pages/payment.astro` | LawPay handoff |
| 404 | `app/not-found.tsx` | `src/pages/404.astro` | |
| 500 | `app/error.tsx` | *(dropped)* | Static output has no server render to fail at; client-side contact-form failure state covers the equivalent case. |
| `/sitemap.xml` | `app/sitemap.ts` | generated by `@astrojs/sitemap` | |
| `/robots.txt` | `app/robots.ts` | `public/robots.txt` | Static, content unchanged |
| OG/Twitter image generators | `app/_open-graph-preview`, `app/_twitter-preview` | `scripts/generate-social-images.mjs` | The old React preview-route approach was dropped for Astro; **2026-07-19** replaced with a standalone Node script (`node scripts/generate-social-images.mjs`) that renders a self-contained HTML composition (brand fonts/images inlined as data URIs) with Playwright's Chromium and screenshots it at the exact `opengraph-image.png` (2400×1262) / `twitter-image.png` (1010×506) dimensions. Re-run the script any time the composition needs updating — see "Open Graph and Twitter images" below. |

## Business facts preserved (now in `src/data/`)

- **Firm**: Ryan Legal, PC. Sole attorney: Russell K. Ryan ("Russ"). Support staff: Crystal Brightwell — title **"Senior Paralegal / Office Administrator"** (corrected 2026-07-19; previously "Legal Support and Office Administration") and tenure **24 years with Ryan Legal** are both confirmed. Not an attorney; never imply she independently provides legal advice.
- **Contact**: (559) 499-4000, russ@ryanlegalpc.com, crystal@ryanlegalpc.com.
- **Address**: 5200 North Palm Avenue, Suite 306, Fresno, California 93704. Map coordinates `36.81224531879936, -119.80423819568016` (as previously placed via `@vis.gl/react-google-maps`).
- **Practice areas** (6): Labor and Employment; Business Transactions and Finance; Health Care; Technology; Business Litigation; Appellate Litigation. Full descriptive copy carried over verbatim into `src/data/practice-areas.ts`.
- **Russ's profile**: summary paragraphs, education (UC Berkeley JD 1988; BYU BS Psychology 1985), bar admissions (CA 1989, UT 2001), court admissions (8 courts incl. U.S. Supreme Court), ratings/designations (Super Lawyer, AV Preeminent, Martindale-Hubbell, etc. — 6 entries with full + simplified text), prior positions (7 entries), public/community service (2 entries), affiliations (7 entries), detailed expertise sections (Employment, Training, Health Care/Business/Real Estate, Litigation — including 6 reported appellate decisions), and client list (24 current, 5 past). All carried over verbatim into `src/data/credentials.ts`, `src/data/clients.ts`, `src/data/people.ts`.
- **Metadata/SEO facts**: page titles, descriptions, keyword lists, `NEXT_PUBLIC_CURRENT_URL`-based canonical URLs, `LegalService`/`WebSite`/`WebPage`/`Person` JSON-LD shapes (schema-dts typed) — structure preserved, now emitted server-side in `BaseLayout.astro` instead of client-injected via `useEffect` (a genuine improvement: crawlers that don't execute JS previously missed this JSON-LD entirely).
- **LawPay destination**: `https://secure.lawpay.com/pages/ryanlegalpc/operating` — preserved exactly, still not embedded (per scope — embed is a future milestone).

## Contact form (preserved behavior, rebuilt implementation)

- Fields: `name`, `phone` (validated as US mobile via `validator.isMobilePhone`), `email` (validated), `description`. All required.
- Original (pre-migration): Next.js Server Action (`'use server'`) + Zod schema + SendGrid dynamic template.
- **2026-07-18 — moved to Resend**: `netlify/functions/contact.ts` now sends via the official Resend Node SDK instead of `@sendgrid/mail`. Same Zod validation, same method/malformed-JSON/field-error handling, same fail-closed behavior and generic public error messages — only the email provider changed. Body content is now a small hand-written text/HTML template in `contact.ts` itself (visitor-controlled fields are HTML-escaped) rather than a SendGrid dynamic template, since Resend doesn't have an equivalent hosted-template feature in play here.
- Recipient (`to:`) and sender (`from:`) are both `info@ryanlegalpc.com`, driven by `CONTACT_TO_EMAIL` / `CONTACT_FROM_EMAIL` — no hard-coded fallback for either. The visitor's own email is set as `replyTo`, never as `from`.
- `ryanlegalpc.com` must be a **verified domain in Resend** before `CONTACT_FROM_EMAIL` will send successfully. The Resend API key should be scoped sending-only and, where Resend's dashboard allows it, restricted to that verified domain.
- Unit coverage for the function lives in `tests/unit/contact.test.ts` (Node's built-in test runner, `npm run test:unit`) — the Resend client is always dependency-injected as a fake in tests, so no automated test run ever sends real email.

## Environment variables

| Old name | Current usage | Notes |
|---|---|---|
| `NEXT_PUBLIC_CURRENT_URL` | `SITE_URL` (build-time only, used in `astro.config.mjs` `site` + canonical/JSON-LD generation) | No longer needs a `PUBLIC_`/client-exposed prefix — it's only read in `.astro` frontmatter (build-time), never shipped to a client bundle. |
| `NEXT_PUBLIC_GOOGLE_API_KEY` | *(dropped for this phase)* | The interactive `@vis.gl/react-google-maps` widget was React-only and out of scope for a React-free foundation. Replaced with a zero-JS Google Maps `output=embed` iframe (no API key required) + the same "Get Directions" link. A richer vanilla-JS map can come back in the redesign phase if wanted. |
| ~~`SENDGRID_API_KEY`~~ | `RESEND_API_KEY` (server-only, Netlify Function) | Provider swap, 2026-07-18 — see contact-form section above. `SENDGRID_API_KEY` can be deleted from Netlify once a deployed Resend test send has been confirmed working (keep it until then as a rollback path). |
| *(new)* | `CONTACT_TO_EMAIL` | Required, no fallback. Currently `info@ryanlegalpc.com`. |
| *(new)* | `CONTACT_FROM_EMAIL` | Required, no fallback. Currently `info@ryanlegalpc.com`; must be on a Resend-verified domain. |

All three (`RESEND_API_KEY`, `CONTACT_TO_EMAIL`, `CONTACT_FROM_EMAIL`) must be set in Netlify for **both** the production context and the `claudeRevamp` branch-deploy context — they are not shared automatically between contexts unless explicitly scoped to "all deploy contexts" in Netlify's environment variable UI. A change to any of them requires a new deploy to take effect (Netlify Functions read `process.env` at deploy/build time, not per-request).

`.env.example` previously contained an unrelated Postgres/NextAuth template left over from the original Next.js "dashboard course" starter — none of those variables were ever used by this app. Removed as dead/misleading.

## Roadmap

**Milestone 1 (this pass, 2026-07-18):** visual redesign implementation — design tokens, dark theme, header/footer, and all five pages rebuilt per the approved Concept Revision 2 direction. `/payment` keeps the existing LawPay handoff link; LawPay is not embedded yet.

> **Milestone 2:** Prototype LawPay's official supported embed inside `/payment`, with an external secure-payment fallback. **Closed 2026-07-19, not pursued** — feasibility research (`docs/lawpay-embed-feasibility.md`) found LawPay's hosted page blocks framing outright and every more-embedded option requires backend infrastructure this static site doesn't have. `/payment` keeps the external handoff link permanently, by decision, not by default.

The broader color/visual-interest pass for Expertise and About, and an updated design audit reflecting Milestone 1 + the cleanup pass, were both completed in the 2026-07-19 "Final visual-interest and color pass" (see below) — visual-design implementation is considered done.

## Pre-launch TODO

Tracked here rather than in code comments so it survives independently of any one file. Update this list as items close out.

- [x] **Russ must approve or revise the Home quotation** — moot as of Milestone 5 (2026-07-21, `fbb3ef4`): the quote section was removed entirely and replaced with a "Values and Mission" section using copy Russ supplied directly (`valuesAndMission` in `src/data/site.ts`). No provisional/drafted-in-his-voice quotation remains on the page.
- [x] **Perform a real deployed Contact-form submission** — Jared confirmed 2026-07-21 the production deploy works and the test email went through.
- [x] **Confirm delivery to `info@ryanlegalpc.com`** — confirmed 2026-07-21 per the same test.
- [x] **Confirm the Reply-To targets the visitor's submitted email**, not the firm's address — confirmed correct 2026-07-21.
- [x] Crystal Brightwell's title ("Senior Paralegal / Office Administrator") and tenure (24 years) — confirmed, no longer provisional.
- [x] Crystal's published email (`crystal@ryanlegalpc.com`) — confirmed 2026-07-19 (verified independently against the legacy `main` branch's `app/content.tsx`) and added to `/contact`.
- [x] **Comprehensive responsive audit** — completed 2026-07-19: fresh-eyes visual/responsive audit across all 14 required viewports × 6 routes × 2 themes (168 combinations), zero horizontal-overflow or clipping defects found. Findings from that audit drove this polish pass; see the published audit artifact referenced in session notes for the full write-up.
- [x] **Final factual and legal copy review with Russ** — Jared confirmed 2026-07-21 this is considered done: Russ sent copy covering all of the previously-drafted/provisional paragraphs (Home values/mission, Russ's About summary, Crystal's About bio — landed in Milestone 5, `fbb3ef4`), and everything else on the site was already factual and previously verified.
- [x] **Russ must approve the rewritten About Training copy** — covered by the same 2026-07-21 sign-off above (Training's figures were already factual/previously-verified content, not drafted-in-his-voice copy).
- [x] **Milestone 2 (LawPay embed) — closed, not deferred.** Researched 2026-07-19 (`a7782d0`, `docs/lawpay-embed-feasibility.md`): LawPay's hosted payment page sends `X-Frame-Options: SAMEORIGIN`, blocking iframing outright; every more-embedded official option (Unified Payment Flow, Hosted Fields, an allow-listed iframe snippet) requires a backend server, partner OAuth, and LawPay-side domain allow-listing the static Astro/Netlify site doesn't have. Verdict: keep the current external handoff link — this is a final decision, not an open task.
- [x] **About page visual break** — added 2026-07-19: `waiting-room-2.png` (optimized to `public/waiting-room.webp`) selected over `waiting-room-1.png` specifically because it has the firm's actual wall-mounted "R | Ryan Legal, PC" sign in frame, giving the page one authentic, on-brand visual moment between the Crystal Brightwell section and the Litigation Record. `waiting-room-1.png` (no visible branding) was the runner-up.
- [x] **Office exterior photo — dropped, not just deferred.** Removed from the repo entirely as of 2026-07-21. Superseded the earlier "confirm it's the right building" framing — even setting the "5250" vs. published "5200 North Palm Avenue" signage mismatch aside, there was no spot in the current design where an exterior shot actually contributed. Not being revisited.
- [x] **Production cutover** — Jared confirmed 2026-07-21 the site is deployed live on the production/main domain, not a branch/preview deploy.
- [x] **Google Search Console — sitemap submitted, indexing requested** — Jared submitted `https://ryanlegalpc.com/sitemap-index.xml` to GSC and requested indexing on all URLs, 2026-07-21.
- [ ] **Privacy Policy content review** — new `/privacy` page added 2026-07-21 (see "Chevron follow-up, contact-form spam protection, security headers, and Privacy Policy" below); still needs Russ's sign-off before being considered final, same bar as the rest of the site's legal/factual copy.
- [ ] **Confirm QC Construction Products' court** — added to About's Reported Decisions 2026-07-21 per Russ's detailed case list, filed under California Courts of Appeal per his email. Its citation (`423 F. Supp. 2d 1008 (D. Ariz. 2006)`) reads as a federal district court in Arizona, not a California appellate court — Jared confirmed filing it as given for now, but this needs a real answer from Russ before launch.
- [x] **Navbar and footer brand marks updated** — 2026-07-19: the header's circular-badge `icon.svg` mark was replaced with `logo-white.webp` (the gold-swoosh-only mark meant for navy surfaces, sitting next to the `Ryan Legal, PC` header text) in `src/components/Header.astro`; the footer's icon-plus-typeset treatment was replaced with the full logo lockup in `src/components/Footer.astro` (now the cropped `footer-logo-white.webp` — see "Cleanup pass" below).
- [x] **Open Graph and Twitter images regenerated (2026-07-19, superseded same day — see "Cleanup pass" below)** — first rebuilt from one shared master composition using the `full-logo-white.webp` lockup, the real Russ portrait, Libre Baskerville/Source Sans 3, and current navy/gold tokens; then simplified further the same day once the first version proved too dense at actual preview size.

## Cleanup pass (2026-07-19, same day as above)

A follow-up pass focused on layout/asset polish flagged by manual review, independent of the broader color/visual-interest work still deferred to a future pass (see Roadmap). All changes left uncommitted per instruction.

- [x] **Flush-footer layout treatment** — `src/components/Footer.astro` takes a `flush?: boolean` prop (`class:list`-driven `.site-footer--flush`, `margin-top: 0`); `BaseLayout.astro` exposes it as a `flushFooter` prop and passes it through. Used on `/` (`index.astro`) and `/404` so the footer sits directly against the preceding navy surface (Home's consultation band, 404's compact content area) with no cream-colored gap. Ordinary pages are unaffected — the footer's normal `margin-top: var(--s9)` still applies. The consultation band and footer remain visually distinct via their existing different navy tokens (`--surface-consult` vs `--surface-navy`), not a new visual device.
- [x] **404 vertical rebalance** — `src/pages/404.astro`'s `<main>` is now a flex column (`:global(main) { display: flex; flex-direction: column; }`) so `.notfound` (`flex: 1`) fills exactly the space between header and (now-flush) footer at any viewport height, centering within the real usable area instead of a guessed `min-height`. A small `min-height: 320px` floor keeps it from collapsing on very short viewports with a tall mobile footer — not an arbitrary large margin.
- [x] **About editorial-width decision** — `.about-section` in `src/pages/about.astro` no longer caps at `var(--content-narrow)` (720px); it now fills the full editorial column (bounded by the page's existing `--content-width` grid). A new `.about-section p { max-width: var(--content-narrow) }` rule keeps actual prose at a comfortable reading measure, while structured content (the credentials grid, archival tables, admissions/client lists, the waiting-room photo break) uses the wider column — which is also why the photo now aligns with the rest of the page instead of overhanging it. `.about-crystal` (Crystal's bio card) got its own `max-width: var(--content-narrow)` so the card doesn't stretch into a wide, sparse box. Chosen over constraining the image, per instruction, after confirming the narrow cap — not the image — was the actual root cause.
- [x] **Footer logo cropped** — `public/full-logo-white.webp` had ~21–25px of optical padding top/bottom vs. ~9–13px left/right at footer render size (measured via bounding-box inspection: source padding L150/T255/R111/B302 px at full 2733×1424 res). New `public/footer-logo-white.webp` crops to content bounds plus a uniform 48px balanced pad (at source res), used only in `src/components/Footer.astro` (`width="230" height="86"`, aspect changed from 1.92:1 to 2.67:1). The original `full-logo-white.webp` is kept — still a legitimate uncropped master, used as the source for the OG/Twitter generator.
- [x] **New transparent navy/gold R mark** — no genuine vector source exists (`icon.svg` was a fake vector: an `<svg>` wrapping one large base64-embedded PNG); `original-images/logo.png`, `public/icon.png`, and `public/favicon.ico` all turned out to be the R mark flattened onto an **opaque white circle**, not a transparent asset. Built `public/r-mark-transparent.webp` by unmatting `original-images/logo.png` against its known solid navy (`#1e275e`)/gold (`#fdb613`) fill colors (per-pixel alpha recovered via the blue channel, edge pixels classified navy-vs-gold by reconstruction error) — clean navy R + gold swoosh/star, no circle, no halo, true alpha edges. For dark-theme use (the mark reads too low-contrast against dark theme's near-black `--bg`), the same unmatte output was recolored navy→`--ivory` (`#edebe3`, the token already used for dark-theme text/links) as `public/r-mark-transparent-on-dark.webp`; a plain white recolor (matching the *existing* `logo-white.webp`, which is gold-swoosh-only, no letterform) was considered and rejected in favor of ivory to match the brand's dark-theme palette. Used on `/404` (`src/pages/404.astro`), theme-swapped the same way `ThemeToggle.astro` swaps its sun/moon icons (two `<img>`s, `display: none` toggled by `:root[data-theme='dark']`).
- [x] **Favicon/icon system** — `icon.svg` deleted (fake vector, see above). `public/icon.png` (96×96) and `public/favicon.ico` (16/32/48 multi-size) regenerated from the same unmatted R mark, transparent background, no white-circle enclosure — tested at rendered 16px/32px against light and dark synthetic browser-chrome colors and judged legible without an enclosure at the time. **Revised 2026-07-19, same-day finishing pass** — real-world use showed the fully-transparent version *did* lose definition against light/white browser chrome; see "Final finishing pass" below for the corrected ivory rounded-square treatment. `apple-icon.png` and the `web-app-manifest-*.png` maskable icons were left untouched: they already use plain opaque square backgrounds (a technical requirement for those formats, not a decorative halo) with generous, already-balanced padding. `BaseLayout.astro`'s icon `<link>`s updated to drop the `icon.svg` reference.
- [x] **OG/Twitter images simplified** — the first regeneration (see entry above) still read as dense at actual feed-preview size. Rebuilt via `scripts/generate-social-images.mjs`: full-bleed navy background, a substantially larger `footer-logo-white.webp` lockup, Russ's portrait as a right-side photo panel (not a small circle), the single positioning statement ("Premier representation for employers, health care providers, and nonprofits throughout California.") in Libre Baskerville, and a small gold "Fresno, California" label. Removed: the long headline, the fake (non-interactive) CTA button, and the site URL. Verified at both full resolution and downscaled to typical feed-thumbnail width (~500px / ~435px) — logo, portrait, and statement all stay legible and hierarchical at preview scale. Same output filenames/dimensions, same metadata references — no changes needed in `BaseLayout.astro` or `json-ld.ts`.
- [x] **Asset cleanup** — `public/thumbnail.webp` deleted (superseded legacy social-preview asset, unreferenced). `public/waiting-room-1.png`, `waiting-room-2.png` (17MB each — the raw/duplicate sources of the already-optimized, already-in-use `waiting-room.webp`), `conference-room.png`, and `russ-desk.png` (unreferenced photo-shoot candidates, see the "Office exterior" item above) moved from `public/` to `original-images/` — preserved for future design work, but no longer shipped in the deployed static output (`public/` dropped from ~107MB to ~3.4MB). `.playwright-mcp/` (Playwright MCP tool output) added to `.gitignore`.
- [ ] **Future consideration, noted but out of scope this pass**: other brand assets currently using plain white (e.g. `logo-white.webp`'s swoosh) may be recolored to `--ivory` in a future pass for palette consistency with the new R-mark treatment — not evaluated or actioned here.

## Final finishing pass (2026-07-19, same day as above)

A narrow follow-up addressing specific items flagged from using the cleanup pass's output. All changes left uncommitted per instruction; still no broader Expertise/About color pass, no LawPay embed work.

- [x] **Dark-theme consultation band — ivory treatment** — `src/pages/index.astro`'s `.close-section` kept its light-theme deep-navy (`--surface-consult`) background unchanged. In dark theme it now flips to `var(--ivory)` (`#edebe3`) with `var(--navy-deep)` heading/body text, a `var(--navy)` primary button (overriding the sitewide `:root[data-theme='dark'] .btn-primary` gold rule, which is otherwise correct for on-dark buttons but wrong once this one surface goes light), a `var(--gold-ink)` rule (the same token the light theme already uses for gold-on-light elsewhere, e.g. the default eyebrow color — not a new color), and a `var(--navy)` focus-ring override (the sitewide dark-theme gold focus ring has only ~1.5:1 contrast against this ivory surface; navy gives ~16:1). `--navy-deep`/ivory body-text contrast is ~16:1, comfortably past AAA — chosen over the mid-tone `--navy` token specifically because `--navy` on `--ivory` measures ~4.3:1, which is fine for the large `h2` but falls just under AA's 4.5:1 for the smaller `.lead` paragraph.
- [x] **404 R mark — responsive sizing** — `src/pages/404.astro`'s `.notfound__mark` is now `width: clamp(72px, 7vw, 96px); height: auto; aspect-ratio: 1/1` (was a fixed 64px). Stays at the 72px floor from 360px up to ~1028px viewport width, scales linearly to the 96px ceiling by ~1371px — i.e. effectively 72px through mobile/tablet, ~96px on laptop/desktop, exactly the "mobile stays close to 72, desktop reaches ~96" split asked for. The light/dark asset swap mechanism is untouched.
- [x] **Favicon — ivory rounded-square treatment** — regenerated `public/favicon.ico` (16/32/48) and `public/icon.png` (96×96) again from a new 512px master: `var(--ivory)` rounded square (20% corner radius — "restrained," not a pill/circle), the navy/gold R mark centered with balanced padding (mark's longer edge fills 68% of the canvas), **plus a 1px-at-32px navy hairline stroke around the square**. The hairline was added after rendering the borderless version at 16/32/48px against synthetic light and dark browser-chrome colors: without it, the ivory square was nearly invisible against white/light-gray chrome (both are close to the same near-white luminance); the navy edge fixed that while staying clearly distinct (and legible) against dark chrome too. This is the "subtle edge... necessary for contrast against light browser chrome" case the brief anticipated — not a reintroduction of the old white-circle halo (square, not circle; hairline, not a filled enclosure ring). `apple-icon.png` and the manifest icons were not touched — no technical or consistency need to.
- [x] **About — Training moved to the narrow prose measure, plus a reusable width utility** — new `.about-narrow { max-width: var(--content-narrow) }` class in `src/pages/about.astro`, applied to: the `#intro` (Russ's profile) and `#training` sections in full (both primarily paragraph-based), the `#crystal` section (consolidating what was previously a one-off `max-width` on the inner `.about-crystal` card — same 720px value, now driven by the shared class instead), and `.about-close`. Removed the now-redundant per-component `max-width` rules on `.about-crystal` and `.about-close` in favor of the one shared class.
- [x] **About — one-column sections evaluated individually** (see width-decision list below) rather than force-fit to one system, per instruction.
- [x] **Social images — logo enlarged** — `scripts/generate-social-images.mjs`'s `.logo` width increased from 46% to 56% of the left column (≈22% larger overall, within the requested 15–25% range). Verified at full resolution and downscaled preview size (~500px/~435px wide) — "Ryan Legal, PC" and the "PC" mark are now clearly legible at preview scale where they were previously borderline. Composition, portrait crop, positioning statement, and "Fresno, California" all unchanged; same filenames/dimensions.
- [x] **Footer/header accessible names verified and corrected** — the footer brand link had no explicit name of its own; its accessible name fell through to the `<img alt={businessName}>` value ("Ryan Legal, PC"), with no indication it's a link to Home. Fixed via Option A: `<a aria-label="Ryan Legal, PC — Home">` + `<img alt="">` in `src/components/Footer.astro`. The header link previously had *both* a redundant `aria-label="Ryan Legal, PC home"` *and* adjacent visible text `Ryan Legal, PC` (the `aria-label`, when present, wins name computation outright and makes the visible text invisible to that computation — not a double-announcement, but redundant and not the intended pattern); removed the `aria-label` in `src/components/Header.astro` so the link's name now comes from the visible `.site-header__brand-name` text, as intended (`img alt=""` was already correct/unchanged).

**About page — final width decision per section:**

| Section | Measure | Why |
|---|---|---|
| Russ's profile (`#intro`) | Narrow | Long-form prose |
| Credentials (`#credentials`) | Wide | 2-column grid (Education + Ratings) — multi-column content |
| Crystal Brightwell (`#crystal`) | Narrow | Bio card, same measure as Russ's profile |
| Waiting-room photo break | Wide | Explicitly the one deliberate wide "chapter break" moment |
| Litigation Record (`#litigation-record`) | Narrow | Prose + a short (6-entry) one-column table — wide would leave long empty gaps before each right-aligned year |
| Training (`#training`) | Narrow | Prose-primary, per instruction |
| Positions (`#positions`) | Narrow | Two short one-column tables (7 and 2 entries) |
| Admissions → Bar Admissions | Narrow | 2 entries (CA/UT), one-column — would be nearly-empty rows at wide measure |
| Admissions → Court Admissions | Wide | 8 entries, already responsive multi-column (`columns: 2 220px`) — benefits from the room |
| Clients & Affiliations → Current/Past Clients | Wide | Long rosters (24 and 5 entries), already multi-column |
| Clients & Affiliations → Affiliations | Narrow | 7 entries, one-column, no per-item metadata — same "empty row" reasoning as Bar Admissions |
| Closing CTA (`.about-close`) | Narrow | Short CTA copy, consistent with the rest of the narrow sections |

Bar Admissions and Affiliations are sub-groups *within* otherwise-wide sections (Admissions, Clients & Affiliations) — only their own heading+list pair got `.about-narrow` (not the whole parent section), so Court Admissions and the client lists keep the wide measure while sitting in the same section. Left edges of all headings still align (only the right edge pulls in for narrow content), so the page doesn't read as arbitrarily disconnected, and the existing subgroup-spacing rule (`.archival-table + h3`, etc.) still matches since it doesn't care about extra classes.

## Targeted cleanup pass (2026-07-19, same day as above)

Narrow follow-up fixing specific items found by manual review of the finishing pass's output. Left uncommitted per instruction; no broader Expertise/About color work, no LawPay embed.

- [x] **Favicon — R mark enlarged (2026-07-19, superseded same day — see "Second targeted cleanup pass" below)** — first regenerated at 80% fill (within the then-requested 78–84% range), chosen by comparing 68/74/78/80/82/84% renders at 16/32/48px against white, light-gray, dark-gray, and near-black synthetic browser-chrome backgrounds. Optical (not geometric) centering established here and carried forward unchanged since. Rounded-corner ratio (20%) and the navy hairline stroke established here too, still unchanged as of the most recent pass.
- [x] **Dedicated on-navy focus treatment** — new `--focus-ring-on-navy: var(--gold)` token in `src/styles/global.css` (theme-invariant — the header/footer navy surface is itself fixed across themes, so a single gold value works everywhere rather than needing per-theme redefinition). Applied via one rule, `.on-navy a:focus-visible, .on-navy button:focus-visible, ... { outline-color: var(--focus-ring-on-navy) }`, layered on the existing sitewide focus rule (which already sets `outline: 2px solid ...; outline-offset: 2px`) — only the *color* changes on navy surfaces, not the shape/thickness/offset. `on-navy` was already present on `<header>` and Home's `.close-section`; added it to `<footer>` itself (previously only its CTA button carried the class) so the one rule covers every footer control without per-element overrides — the footer CTA's now-redundant own `on-navy` class was removed since the ancestor supplies it via the existing descendant selectors. This fixes the light-theme header/footer/close-section case (default `--focus-ring` is dark navy — near-invisible on the navy surface) and keeps dark theme's already-gold ring consistent and explicit rather than incidental. The 2px outline + 2px offset means the ring always renders in the surrounding-surface gap outside each element's own box, so it works correctly on the gold CTA button too (ring sits in the navy gap around the button, not on the gold face) without needing a separate ivory variant.
- [x] **Home practice-area links — stronger hover/focus treatment** — `.practice-grid__item h3 a` in `src/pages/index.astro` changed from a 1px `border-bottom` to the same 2px treatment as `.link-editorial` (the hero's "See areas of practice" link): `border-bottom: 2px solid transparent` + `padding-bottom: 1px`, resolving to `var(--gold)` (not `--gold-accent`) on hover/focus, matching `.link-editorial` exactly rather than approximately. No layout shift (the border box is reserved at 2px whether transparent or gold), no markup/component changes.
- [x] **`.about-narrow` implementation reviewed, left unchanged** — inspected the sibling-class subgroup pattern (Bar Admissions, Affiliations: separate `.about-narrow` on the `<h3>` and the following `<ul>` rather than a wrapping element). Conclusion: **sound, no change made.** Both elements are direct children of the same containing block with identical `margin-left: 0` and the same absolute `max-width: var(--content-narrow)` (not a percentage), so their right boundaries are mathematically identical — there's no way for them to drift apart under the current CSS, contrary to what "separately classed elements" might suggest at a glance. A wrapper-based refactor was considered (the brief explicitly permitted it if it would be cleaner) but rejected: the only real argument for it is a hypothetical future-maintenance slip (someone adding a new narrow subgroup and forgetting the class on one of the two siblings), which isn't a concrete problem today, and a wrapper would break the existing sibling-based subgroup-spacing selector (`.archival-table + h3`) unless the selector and every subgroup's markup were refactored together — not justified to fix a theoretical risk. Section-transition dividers (`.about-section`'s `border-top`) are full-width regardless of the content's measure, so narrow/wide transitions read as intentional structural breaks rather than accidental width changes.
- [x] **Accessibility verification** — three new Playwright tests (decorative-alt + on-navy focus-color resolution in both themes; keyboard order through the skip-link → header brand link, plus every footer link individually reachable; mobile-menu Escape closes and returns focus to the toggle). Header/footer accessible-name tests from the finishing pass re-verified unchanged (header names from visible text, footer via `aria-label`).
- [x] **Incidental: Home H1 updated (2026-07-19, superseded same day — see below)** — first changed to "Practical counsel for everyday decisions. Strong representation when the stakes are high.", per direct instruction mid-pass. Revised again the same day — see "Second targeted cleanup pass" for the final, approved wording.
- [x] **Incidental: OG/Twitter "Fresno, California" enlarged** — `.locale` in `scripts/generate-social-images.mjs` increased `1.35vw → 1.9vw` (~41%, within the requested 35–50%), letter-spacing nudged down `0.1em → 0.08em` so the larger glyphs don't read as over-spaced. Verified legible at ~500px/~435px preview widths, still clearly smaller/secondary to `.statement`, same gold color/position/margin, same composition otherwise. Regenerated both images at existing filenames/dimensions. **This composition, including the locale-text size, is now final — not reopened in the pass below.**

## Second targeted cleanup pass (2026-07-19, same day as above)

Narrow follow-up covering final Home H1 wording/verification, an Expertise heading change, a further favicon size increase, an `.on-navy` cleanup, and a wrapped-underline fix. Left uncommitted per instruction; no broader Expertise/About color work, no LawPay embed, OG/Twitter composition not reopened.

- [x] **Home H1 — final, approved wording** — the user manually set it to **"Practical day-to-day counsel. Strong representation when the stakes are high."** (`src/pages/index.astro`). **This exact wording must not be reverted or edited in future passes** — not to "Direct access to one attorney...", not to "Practical counsel for everyday decisions...", and not to any version substituting "advocacy" for "representation". `tests/smoke.spec.ts`'s heading assertion updated to match (`'Practical day-to-day counsel'`). Focused spacing review at 390×844, 768×1024, 1280×720, and 1440×900 (line counts measured via a DOM Range over the heading text, not eyeballed): **4 lines at all four reviewed sizes** (`.hero__copy h1 { max-width: 16ch }`, unchanged) — no orphaned words, no collision with the portrait/CTA/whitespace, no clipping at any reviewed size. No width, font-size, line-height, or spacing change was needed; the existing hero CSS already accommodates the new copy cleanly.
- [x] **Expertise page H1 changed to "Areas of Practice"** (`src/pages/expertise.astro`, was "Where Ryan Legal focuses") — Russ's preferred client-facing terminology. Navbar label, route, metadata `<title>`/JSON-LD `name` fields, intro paragraph, numbering, rail, jump nav, and responsive behavior all untouched — only the literal `<h1>` text changed. `tests/smoke.spec.ts` and a new `polish-pass.spec.ts` test updated/added to match.
- [x] **Favicon — R mark enlarged again, 80% → 84%** — still had slightly more empty ivory space than necessary. Compared 80/82/83/84/85% at 16/32/48px against the same four chrome backgrounds as the prior pass; 84–85% were both visibly bolder with clean breathing room around the star, swoosh, R, and lower curve (no antialiased-pixel merging with the border) — finalized at **84%** (the suggested target; 85% wasn't a clear enough improvement to justify going past it). Optical centering (not geometric) retained — recomputed for the new scale, ink centroid still lands within <1px of true canvas center. 20% corner radius and the navy hairline border unchanged. Regenerated `public/favicon.ico` (16/32/48) and `public/icon.png` (96×96); navbar, 404, Apple, and manifest icons untouched.
- [x] **`.on-navy` removed from the Home consultation section** — `.on-navy` is reserved for surfaces that are navy in *both* themes (header, footer); the consultation section is navy in light theme but ivory in dark theme, so the class was semantically wrong there and made its focus-ring color depend on `.on-navy`'s cross-cutting rule plus a higher-specificity override to undo it in dark mode. `<section class="close-section on-navy">` → `<section class="close-section">` in `src/pages/index.astro`. Replaced with: a local `--close-focus-color` custom property (`var(--gold)` by default on `.close-section`, redefined to `var(--navy)` inside the existing `:root[data-theme='dark'] .close-section` rule) referenced by one unconditional `.close-section a:focus-visible { outline-color: var(--close-focus-color) }` rule — no per-theme selector duplication needed for the focus color itself. Also added an explicit `.close-section .btn-primary` gold-background rule (replicating what `.on-navy .btn-primary` used to provide), since removing the class would otherwise have silently reverted the light-theme button to the sitewide default navy button — the existing more-specific dark-theme button override was untouched and still wins in dark mode. Background, typography, spacing, and content of the section are unchanged; header and footer keep `.on-navy` as before.
- [x] **Home practice-area links — wrapped-underline fix** — the 2px `border-bottom` treatment from the prior pass looked fine on one line but produced a rigid, poorly-spaced bar on wrapped titles (e.g. "Business Transactions and Finance"), since a border draws once under the element's whole box rather than per line. `.practice-grid__item h3 a` (`src/pages/index.astro`) switched to a real text-decoration underline: `text-decoration-line: underline`, `text-decoration-color: transparent` (default, becoming `var(--gold)` on hover/focus — same transparent-then-colored pattern as before, so still zero layout shift, since text-decoration never affects box layout at all), `text-decoration-thickness: 2px`, `text-underline-offset: 0.18em`, `text-decoration-skip-ink: auto`. Browsers draw text-decoration per line automatically, so every wrapped line gets its own correctly-spaced underline with no markup changes. These links have no arrow/icon inside them (unlike `.link-editorial`), so no dedicated text-span wrapping was needed. `.link-editorial` itself (the hero's "See areas of practice" link) was left on its existing border-based implementation, as instructed — the two don't need to share implementation to match visually.
- [x] **Accessibility verification** — new Playwright tests: consultation CTA resolves to gold outline in light theme / navy outline in dark theme and confirms the section no longer carries `.on-navy`; practice-area link underline is present-but-transparent by default and turns gold on focus, checked on both a single-line and a wrapped title; Expertise H1 text plus navbar "Expertise" label both verified in one test. Existing header/footer accessible-name, alt="", and keyboard-order tests re-verified unchanged (nothing about header/footer focus or naming changed this pass).

## Assets preserved as-is

Astro uses the same `public/` convention as the old app. Current `public/` contents (as of the 2026-07-21 unused-asset pass):
`icon.png`, `favicon.ico`, `apple-icon.png`, `r-mark-transparent.webp`, `r-mark-transparent-on-dark.webp`, `full-logo.webp` (color source of truth, referenced by JSON-LD), `footer-logo-white.webp`, `logo-white.webp`, `square-profile.webp`, `crystal-profile.webp`, `waiting-room.webp`, `opengraph-image.png`, `twitter-image.png`, `manifest.json`, `web-app-manifest-*.png`, `robots.txt`. `icon.svg` and `thumbnail.webp` were removed 2026-07-19; `waiting-room-1.png`, `waiting-room-2.png`, `conference-room.png`, `russ-desk.png` moved to `original-images/` the same day.

**2026-07-21 unused-asset pass:** cross-referenced every file in `public/` against real code usage (`src/`, `astro.config.mjs`, `netlify.toml`, `scripts/`, `public/manifest.json` — not just docs mentions, which can go stale). Three files had zero references anywhere in the actual build and were moved to `original-images/`: `full-logo-white.webp` (superseded — `scripts/generate-social-images.mjs` switched to `footer-logo-white.webp` in an earlier pass, but the file itself was never deleted), `av-preeminent.png`, and `super-lawyers-badge.png` (both award-badge graphics; the AV Preeminent/Super Lawyers proof points on Home/About have always been rendered as styled text, never these images). `office-exterior.jpg` and `av-preeminent.png`/`super-lawyers-badge.png` no longer appear in `public/` at all as of this pass (the office-exterior photo was dropped entirely per owner decision, not just moved). A stray `.DS_Store` that had been silently shipping into every production build (Astro copies `public/` verbatim, `.gitignore` doesn't affect that copy) was also deleted — harmless in practice since real deploys build from git, where it was never tracked, but worth clearing locally.

`original-images/` (full-resolution untracked sources) and the design-phase deliverables (`Ryan Legal, PC — *.html/.pdf`) were otherwise left untouched, per instruction.

## Deliberately excluded / not carried forward

- All React components, JSX/TSX, and the entire `app/ui/*.css` system — presentational, and the instruction was explicit that these are disposable regardless of quality.
- `react-transition-group` animation classes, the custom roving-`tabindex` carousel/tab implementations for About and Expertise, and the hand-rolled mobile-menu focus trap — all Next/React-specific interaction code. The redesign phase will need new, simpler disclosure/menu interactions built as small native scripts.
- `nuqs` (URL query-state library) — was used to mirror form input and the About/Expertise "topic"/"expanded" tab state into the URL. Dropped; not needed without the old tab UI. If deep-linkable Expertise sections are wanted later (the design brief calls for this), that can be done with plain anchor hashes.
- `@vis.gl/react-google-maps`, `@fortawesome/*`, `react`, `react-dom`, `react-transition-group`, `schema-dts` (kept the *shapes* schema-dts described, but not the package — JSON-LD objects are now plain typed literals in `src/data/`), `next`, `@next/bundle-analyzer` — all removed from dependencies.
- `pnpm-lock.yaml` — removed; project now uses npm per the new foundation's requirement, fresh `package-lock.json` generated.
- Stale `.env.example` Postgres/Auth.js template (see above).

## Structured-data & technical SEO pass (2026-07-19, Agent B — orthogonal to concurrent visual work)

Focused JSON-LD and metadata audit. No visible design, copy, navigation, Contact/LawPay behavior, or social-image work touched. Left uncommitted per instruction.

### Original JSON-LD structure and problems found

Emitted from `src/lib/json-ld.ts`, rendered by `src/layouts/BaseLayout.astro` as one `<script>` tag per array entry (Home/Expertise: `LegalService` + `WebSite` + `WebPage`; About: `Person` (Russ) + `WebPage`; Contact/Payment: `WebPage` only; 404: none). Problems found:

- **No Crystal Brightwell entity anywhere** — the primary gap this pass closes.
- `legalServiceJsonLd()` claimed `acceptedPaymentMethod: Cash, Check`, contradicting `/payment`'s actual visible copy ("Major credit and debit cards, processed securely by LawPay").
- `openingHours: 'Mo-Fr 08:00-17:00'` was asserted but never published anywhere in visible site content — an unsupported claim.
- Russ's `alumniOf` said "University of California, Berkeley **School of Law**"; `src/data/credentials.ts` (the source of truth) says "University of California, Berkeley" with no "School of Law" suffix.
- `russRyanPersonJsonLd()`'s `worksFor` re-embedded `name`/`url` duplicate of the firm entity rather than a plain `@id` reference — a second, driftable copy of the same facts.
- Home's `webPageJsonLd()` breadcrumb was a single-item list (`Home` alone) — provides no navigational value and is generally discouraged.
- Contact's breadcrumb labeled the page **"Contact Us"** while the nav (`src/data/site.ts` `navLinks`) and the client-facing route both say **"Contact"** — a metadata/nav terminology mismatch.
- Each page emitted several *separate* top-level JSON-LD objects (each with its own `@context`) rather than one connected `@graph` — valid, but harder to reason about as a single entity graph.
- 404 had no `noindex` signal (relies solely on the real HTTP 404 status).

Nothing else was found broken: `@id` values were already stable and consistently reused, `SITE_URL` was already a single source of truth shared by `astro.config.mjs` (canonical `site`), `src/data/site.ts` (JSON-LD/canonical), and `@astrojs/sitemap` — no localhost/preview-URL leakage risk existed structurally, just needed verifying (see Validation below).

### Final connected entity graph

Each page now renders **one** `<script type="application/ld+json">` containing a single `@graph` (see `buildGraph()` in `src/lib/json-ld.ts`), built from the same per-entity functions as before (now `@context`-free, since only the graph root needs one):

- **Home** (`/`): `LegalService` (firm) + `WebSite` + `WebPage` (`mentions` → Russ, Crystal — both are named/quoted on the page).
- **Expertise** (`/expertise`): `LegalService` (firm) + `WebPage`. No service/product sub-entities added (page describes practice areas, not purchasable services).
- **About** (`/about`): `Person` (Russ, full) + `Person` (Crystal, full) + `AboutPage` (`mainEntity` → both people). This is the *only* page that fully defines the two Person entities — every other reference to them elsewhere on the site is a bare `{"@id": ...}` pointer, never a redefinition.
- **Contact** (`/contact`): `ContactPage` (`mainEntity` → firm, `mentions` → Russ, Crystal by `@id`; no LawPay/payment entity here).
- **Payment** (`/payment`): `WebPage` only — no firm/person entity redefinition; nothing in its graph names LawPay, so no ownership/operation is implied.
- **404**: no JSON-LD (nothing indexable to describe) + `<meta name="robots" content="noindex, nofollow">`.

### Crystal Brightwell — structured-data representation

`crystalBrightwellPersonJsonLd()` in `src/lib/json-ld.ts`, emitted only on `/about`:

```json
{
  "@type": "Person",
  "@id": "https://ryanlegalpc.com/about#CrystalBrightwell",
  "name": "Crystal Brightwell",
  "url": "https://ryanlegalpc.com/about",
  "jobTitle": "Senior Paralegal / Office Administrator",
  "description": "Senior Paralegal / Office Administrator, with Ryan Legal, PC for over 24 years.",
  "email": "crystal@ryanlegalpc.com",
  "worksFor": { "@id": "https://ryanlegalpc.com#RyanLegalPC" }
}
```

Deliberately **not** included: `Attorney`/legal-professional typing, credentials, admissions, awards, alumni data, phone (the shared firm line isn't hers individually), image, or social links. Her email is her own already-published address (`src/data/site.ts` `contact.emails.crystal`, shown on `/contact`) — not invented. The `description` restates the 24-years figure exactly as published on Home (`index.astro`) and About (`about.astro`, `.about-crystal__meta`) — not a new claim.

### Russ Ryan — final representation

`russRyanPersonJsonLd()`, unchanged in shape except: `worksFor` now a bare `{"@id": "...#RyanLegalPC"}` reference (was a partial duplicate object), and `alumniOf` institution names now generated from `src/data/credentials.ts` `education` verbatim (fixes the invented "School of Law" suffix). Single canonical `@id` (`/about#RussellRyan`), referenced — never redefined — from Home's `mentions`, Contact's `mentions`, and the firm's `founder`. `jobTitle: "Founder and Owner"`, `honorificSuffix: "JD, Esq."`, awards/admissions all sourced from `src/data/credentials.ts`.

### Firm entity — selected type

Kept **`LegalService`** (a schema.org `LocalBusiness` subtype) rather than stacking additional types — it's the most specific supported type that fits a solo-attorney law practice with a physical Fresno office, and was already correctly chosen pre-pass. Fixes applied: `acceptedPaymentMethod` corrected to Credit Card / Debit Card (matches `/payment`); `openingHours` removed (unsupported); added `employee: [{"@id": crystal}]` alongside the existing `founder: {"@id": russ}` — Russ is owner/founder, Crystal is staff, so the two relationships aren't interchangeable. `serviceArea: California, Utah` kept as-is — supported by `src/data/credentials.ts` bar admissions (CA 1989, UT 2001), not invented.

### Stable `@id` conventions

Centralized as exported constants in `src/lib/json-ld.ts` (previously inlined as string literals per call site):

```
firm:    `${siteUrl}#RyanLegalPC`
website: `${siteUrl}/#website`
Russ:    `${siteUrl}/about#RussellRyan`
Crystal: `${siteUrl}/about#CrystalBrightwell`
pages:   `${siteUrl}<path>#webpage`
```

All resolve against `SITE_URL` (`src/data/site.ts`), the same single source of truth used by `astro.config.mjs`'s `site` (canonical URLs, `@astrojs/sitemap`) — one env var controls every URL surface. **To add/change staff later**: add a person object to `src/data/people.ts`, a `<name>PersonJsonLd()` builder + exported `<name>Id` constant to `src/lib/json-ld.ts` following the Crystal pattern, then reference the `@id` (never redefine the object) from any other page that mentions them.

### Page-level schema and breadcrumb decisions

- Home: no breadcrumb (single-item "Home alone" trail removed — `webPageJsonLd()` now omits `breadcrumb` entirely when the array has ≤1 entry).
- Expertise/Payment: kept generic `WebPage` — no more specific schema.org type gives a clear benefit without over-specializing.
- About → `AboutPage`, Contact → `ContactPage` — both are standard, well-supported specializations with a clear semantic match to the page content.
- Breadcrumb labels now match nav/client-facing terminology exactly (Contact fix, below). No breadcrumbs added anywhere they didn't already exist.

### Metadata / canonical corrections made

- Contact page breadcrumb label: **"Contact Us" → "Contact"** (matches `navLinks` and the route; `/contact-us` remains only a 301 redirect, never a competing canonical or breadcrumb label).
- `acceptedPaymentMethod` (Cash/Check → Credit Card/Debit Card) — see above.
- `openingHours` removed — see above.
- `alumniOf` institution names corrected to match `src/data/credentials.ts` verbatim.
- Added `<meta name="robots" content="noindex, nofollow">` to 404 (`BaseLayout.astro` gained an optional `noindex` prop) — defense in depth alongside the real HTTP 404 status already served.
- Conventional metadata (`<title>`, description, canonical, OG/Twitter, keywords) was audited page-by-page and found already coherent with visible H1s/JSON-LD — no changes needed there. Home's approved H1 ("Practical day-to-day counsel...") and Expertise's approved H1 ("Areas of Practice") were not touched; their JSON-LD `name`/`description` fields already didn't reference the old headings.

### Unsupported or risky properties deliberately omitted

`aggregateRating`/reviews, price ranges, founding dates, social profiles, geographic coverage beyond CA/UT, Crystal's credentials/admissions/awards/image/phone/bio beyond the published tenure line, any `Attorney` typing for Crystal, and any LawPay entity/ownership claim on `/payment`.

### Files changed

`src/lib/json-ld.ts` (Crystal entity, `@id` constants, `buildGraph()`, accuracy fixes), `src/layouts/BaseLayout.astro` (single `@graph` script tag, `noindex` prop), `src/pages/index.astro`, `src/pages/about.astro`, `src/pages/contact.astro`, `src/pages/404.astro` (JSON-LD/metadata wiring only — no markup/CSS touched), `tests/seo-structured-data.spec.ts` (new). `src/pages/expertise.astro` and `src/pages/payment.astro` were audited but needed no changes.

### Validation performed

- `astro check` — 0 errors/warnings/hints.
- `astro build` — succeeds; also re-run with `SITE_URL=https://ryanlegalpc.com` explicitly (local `.env` defaults it to `http://localhost:3000` for dev) and confirmed `grep -r localhost dist/` finds nothing — no dev/preview URL leakage in a production-value build.
- Every page's rendered `dist/**/index.html` `<script type="application/ld+json">` parsed with `JSON.parse` by hand — all valid, no duplicate `@id`s within a page, all `@id`s share the canonical origin, `WebPage.url` matches `<link rel="canonical">`.
- `dist/sitemap-0.xml` includes exactly the 5 indexable routes; `/404` correctly excluded.
- New Playwright spec `tests/seo-structured-data.spec.ts` (run via `npx playwright test tests/seo-structured-data.spec.ts --project="Desktop Chrome"`, single project/theme per instruction): 8/8 passing — graph parses + origin/`@id`-uniqueness/canonical-agreement on all 5 pages, Crystal typed `Person` with exact title and never `Attorney`, 404 `noindex` + no JSON-LD, `/payment` graph never mentions LawPay.
- **Validator limitation**: no internet-connected external validator (Google Rich Results Test / schema.org validator) was reachable from this environment — local validation (JSON parsing, schema shape review against schema.org property definitions, manual cross-reference against schema.org type/property docs from training knowledge) was used instead. Valid, parseable JSON-LD does not guarantee a Google rich result; nothing here claims eligibility for any specific rich-result type.
- `npm ci`, `npm ls`, `npm audit`, and the unit-test suite were intentionally skipped this pass (no dependency or runtime-logic changes) per instruction.

### Remaining project tasks (not completed this pass)

- LawPay embed feasibility — separate task, untouched.
- Broader Expertise/About visual-interest pass — pending (visual, out of scope for this pass).
- Updated final design audit — pending.
- Real deployed Contact-form test — pending (no real email was sent this pass).
- Russ's Home blockquote approval — pending.
- Final factual/legal review of all structured-data claims — pending (everything above was cross-checked against existing `src/data/*` and visible copy, but has not had an attorney/owner sign-off).
- Production cutover — pending.

### Remaining factual questions for owner confirmation

- Should Crystal's `@id`/canonical anchor live at `/about#CrystalBrightwell` (chosen to mirror Russ's existing `/about#RussellRyan` convention), or would a dedicated staff URL be preferred if one is ever added?
- Is Utah appropriately still a `serviceArea` for the firm-level entity (supported by bar admission, not by the homepage slogan text, which says "throughout California" only) — kept as-is since it predates this pass and is factually supported, but worth an explicit owner call given the slogan's California-only framing.

## Final visual-interest and color pass (2026-07-19, same day as above)

Implementation pass following the independent design audit (`docs/final-design-audit.md`, ~8/10 overall), which recommended better distributing the existing navy/gold/ivory palette across the pages that still read as visually quiet or document-like rather than any structural repair. **This is intended to be the final visual-design implementation pass** — the recommendation after this is to stop visual polishing and move to content/legal review and launch prep. No new pages, sections, photography, or accent colors were introduced; no Training content, About narrow/wide logic, LawPay flow, or approved Home/Expertise H1s were touched. Left uncommitted per instruction.

- [x] **Expertise — new closing CTA band.** The page previously ended right after the sixth practice area with no closing moment. Added a full-width `.closing-band` section (see shared-component note below) as a sibling of `.exp-layout`, not nested inside it — nesting would have constrained it to the content column instead of full bleed. Heading "Ready to Discuss Your Matter?", supporting line callbacks to the intro's own "day-to-day advising to trial and appeal" language rather than inventing a new claim, CTA "Get in touch" → `/contact`. `BaseLayout` given `flushFooter` so the band sits flush against the footer, matching Home's pattern. The band carries no `id` and no rail entry, so `scroll-spy.ts` (which only ever observes ids referenced by an `[data-spy-rail] a[href="#..."]` link) has nothing to attach to there — confirmed the rail still lists exactly six items.
- [x] **Expertise — visual-rhythm devices (low-intensity, no midpoint band).** Considered per the brief: a bolder chapter-number treatment, more deliberate gold at section openings, section-divider rhythm, a raised/tinted treatment around the client-type/case-record metadata, primary-vs-supporting content distinction, and rail/active-state enhancements. **Implemented two, combined into one repeatable "chapter opening" language:**
  1. `.exp-section__num` (the "01"–"06" markers) gained a short 22px gold rule immediately before the numeral (CSS `::before`, no markup change) and a slightly larger size — turns a small numeral into a real chapter-opening cue, reusing the same "short gold rule" motif already used elsewhere on Home/About rather than inventing a new one.
  2. `.exp-compare` (the Client Types / proof-callout cluster inside each section) gained a `--bg-raised-2` tinted background and rounded corners, distinguishing supporting metadata from the primary explanatory paragraph above it — directly one of the brief's suggested directions. Its grid switched from a fixed `1fr 1fr` to `repeat(auto-fit, minmax(240px, 1fr))` so the 4 of 6 sections that define only `proof` *or* only `clientTypes` (not both) fill the card edge-to-edge instead of leaving an empty second column inside the new tinted panel.
  
  **Why not a midpoint full-width navy band (explicitly rejected by the brief):** a second strong navy moment mid-page would have competed with the new closing band for "most important thing on the page," diluted the header/footer/consultation-band navy's status as the site's reserved strong-color move, and interrupted the six-section reading rhythm the numbered rail depends on. Both implemented devices are section-local (not full-bleed), reuse existing tokens/motifs instead of adding anything new, and are restrained enough to repeat five (compare) or six (numeral) times without fatiguing — verified against the full six-section render in both themes.
- [x] **About — closing section replaced with the shared closing band.** The previous `.about-close` was a quiet narrow section inside `.about-main` (bounded by `.about-layout`'s `max-width`). Removed and replaced with the same `.closing-band` pattern as Expertise, as a sibling of `.about-layout` for full bleed. Heading/lead text preserved exactly ("Talk with Russ directly" / "Every matter is handled by the attorney you meet — not passed down the chain."); only the CTA label changed, "Contact Us" → "Get in touch", so all three long-form pages (Home, Expertise, About) now end on the same CTA language while keeping page-specific headings/copy. `BaseLayout` given `flushFooter`. `railSections` (the About rail's 8 entries) untouched — confirmed still exactly 8.
- [x] **Shared closing-band component decision: shared CSS, not a shared Astro component, and Home's own implementation was left alone.** A `.closing-band` class was added to `src/styles/global.css` (navy-in-light/ivory-in-dark, gold rule, theme-aware focus color via `--band-focus-color`, gold-button styling) — visually identical to Home's existing `.close-section`, but a parallel twin rather than an extraction of it. Home's page-scoped `.close-section` CSS in `index.astro` was **not** touched or migrated, so the already-approved Home band carries zero risk from this change; Expertise and About both adopt the new shared class instead of duplicating ~60 lines of CSS twice more. This matches the brief's own fallback guidance ("if extraction introduces risk or needless complexity, keep shared CSS and separate markup instead").
- [x] **Home quotation — restrained pull-quote treatment.** Previously plain italic blockquote text with no visual distinction from surrounding copy — the flattest section on the page. Wrapped the blockquote + attribution (not the Crystal-scheduling line below, which stays plain) in a new `.quote-section__mark`: a 3px gold-accent left rule plus a soft one-sided `linear-gradient(to right, var(--bg-raised-2), transparent 85%)` wash — no enclosing border, no border-radius, so it reads as editorial emphasis rather than a UI card. Quotation wording, attribution, and the Crystal line are all unchanged verbatim.
- [x] **Contact — information column given selective brand character.** `.contact-main__info` (phone/email/office block) gained a `var(--bg-raised-2)` panel with rounded corners and a 3px gold top edge — the same "gold marks an opening" language as Expertise's new chapter markers — plus a `Reach us directly` eyebrow (existing `.eyebrow` styling, no new component; `.about-photo-break` already establishes the precedent of a standalone eyebrow with no adjacent heading). The form column (`.contact-main__form`) was not touched. The first `.contact-block`'s own `border-top` was suppressed inside the new panel only, since the panel edge plus the eyebrow already provided separation and the pre-existing per-item rule read as a doubled divider directly under it.
- [x] **Dark-theme parity** — verified all of the above at 1280×800 and 390×844 in both themes via Playwright MCP screenshots: the Expertise/About closing bands flip to the same strong ivory-on-dark inversion established on Home; the raised `.exp-compare` and Contact info panels use `--bg-raised-2` (a lighter navy than the page background, not a "barely visible dark rectangle"); the Home quote wash reads as a moody navy/gold pull-quote rather than muddy; gold-accent rules and focus rings all resolved correctly in dark mode without any dark-only visual language that isn't also present in light mode.
- [x] **Training untouched** — `#training`'s markup, copy, and `.about-training-list` structure in `src/pages/about.astro` were not part of this pass's diff at all; only the closing section below it (a separate, later `<section>`) changed.
- [x] **LawPay** — no embed work performed; the external `/payment` link and copy are unchanged, consistent with the closed embed-feasibility milestone.

### Files changed

`src/styles/global.css` (new shared `.closing-band` block), `src/pages/expertise.astro` (closing band markup + `flushFooter`, `.exp-section__num` chapter-rule, `.exp-compare` raised/auto-fit treatment), `src/pages/about.astro` (`.about-close` → shared `.closing-band` + `flushFooter`, removed now-unused `.about-close` rule), `src/pages/index.astro` (`.quote-section__mark` wrapper + pull-quote CSS), `src/pages/contact.astro` (`.contact-main__info` raised panel + eyebrow, first-item divider suppression), `tests/polish-pass.spec.ts` (5 new tests covering the closing bands' flush-footer/CTA/rail-exclusion, their focus-ring colors in both themes, the Home quote wrapper, and the Contact eyebrow). `docs/final-design-audit.md` was not modified.

### Validation performed (Node 24, via nvm — the shell's default `node` resolved to v22 and can't run the `.test.ts` unit files directly)

- `npm ci` — clean install from the lockfile, 0 vulnerabilities.
- `astro check` — 0 errors/warnings/hints.
- `tsc --noEmit` — 0 errors.
- `eslint .` — 0 errors/warnings.
- `astro build` — succeeds (run twice: once before `npm ci`, once after, to confirm the reinstall changed nothing observable).
- `node --test tests/unit/**/*.test.ts` — 11/11 passing.
- `npx playwright test tests/polish-pass.spec.ts tests/smoke.spec.ts tests/seo-structured-data.spec.ts` (Desktop Chrome + Mobile Safari, against the production build, not a dev server) — 71/71 passing, 1 pre-existing WebKit skip (Tab-to-plain-links, documented in the test itself, unrelated to this pass).
- `npm ls` — dependency tree intact, nothing missing/extraneous.
- `npm audit` — 0 vulnerabilities.
- Focused visual review via Playwright MCP screenshots at 1280×800 and 390×844, light and dark, on Home/Expertise/About/Contact only (per instruction — not the full prior-pass viewport matrix): no overflow/clipping, no layout shift, no closing-band/footer collision, rail/scroll-spy unaffected, the About waiting-room photo renders correctly in-viewport (a blank capture only appeared in the stitched full-page screenshot, a lazy-load screenshot-timing artifact unrelated to this pass's changes, not a real regression).

### Remaining project tasks (not completed this pass)

- Russ's Home blockquote wording approval — still pending (this pass changed the quotation's presentation only, not its wording).
- Real deployed Contact-form test — pending (no real email was sent this pass).
- Final factual/legal review of all visible copy and claims — pending.
- Production cutover — pending.
- Training section content/structure — intentionally left dense pending Russ's own review; not reopened this pass.
- LawPay embed — feasibility research already closed as a separate milestone; the external flow was left unchanged and is not being revisited.
- Per the design audit and this pass's own brief: **visual-design implementation is now considered complete.** Any further changes should come from owner/content feedback rather than another open-ended polish pass.

## Follow-up: dark-theme secondary accent + About Bar Admissions (2026-07-19, same day as above)

Two small approved follow-ups to the pass above, discussed and confirmed with the owner before implementation.

- [x] **About — Bar Admissions compacted.** Was an `archival-table` (bigger type, full-width `border-top`, one column) sized for a 2-item list — left an awkward amount of empty space. Now `<ul class="multi-column-list bar-admissions-list">` (`src/pages/about.astro`), same `columns: 2 220px` setting as the adjacent Court Admissions list, each entry a single plain-text string ("California, 1989" / "Utah, 2001" — comma chosen over a hyphen, which could misread as a range between a place and a year). First tried a two-span flex `space-between` row per item to preserve a label/meta visual split; dropped after visual review — at this column width the space-between pushed the year ~200px away from the state name, an awkward gap for only two words of content. `.about-narrow` removed from both the heading and list (no longer needed at the new compact width). `tests/polish-pass.spec.ts`'s existing About-width test updated to also assert Bar Admissions now measures `none` (wide), not narrow.
- [x] **Dark theme — proper secondary accent, not just a lighter gold.** `--gold-ink` (`#8a5a00`, light theme's muted-brown eyebrow/chapter-number color) never had a dark-theme value; a prior pass had patched around it by falling back to `--gold-accent` for `.eyebrow` and Expertise's `.exp-section__num`/`.exp-index__num` in dark mode specifically, since gold-ink only reaches ~3.2:1 against the dark `--bg` (fails the 4.5:1 text minimum). Invisible where these elements appear once, but on Expertise the eyebrow plus six repeated chapter numbers plus the sticky rail's own six numerals all fire at once, reading as "a lot of gold." First tried simply lightening gold-ink's same hue (~39°) for dark theme — visually inconclusive, read as "gold, but dimmer" rather than a distinct second color. Landed on a genuine hue shift instead: **`--gold-ink: #c67139` in dark theme** (hue ~24°, bronze/copper rather than yellow-gold), ~5.3:1 contrast against `--bg` — legible, and visually distinct from `--gold-accent` at a glance rather than a value-shifted twin. Implemented as the token itself (`:root[data-theme='dark']` and its `@media (prefers-color-scheme: dark)` mirror, `src/styles/global.css`), with the ad hoc `.eyebrow`/`.exp-section__num`/`.exp-index__num` dark-theme overrides removed so they fall through to the new token — this is sitewide (Home/About/Contact eyebrows included), not Expertise-scoped, since the underlying token was the actual gap.
  - **Exception carved out and hardcoded:** Home's `.close-section` and the shared `.closing-band` also reference gold-ink in dark theme, but for an unrelated reason — their surface flips to ivory in dark mode (the one deliberately-inverted "destination" moment on each page), so they need gold-ink's *light-surface* legibility (~5:1 against ivory), not the new dark-navy-surface bronze (which only reaches ~3.0:1 against ivory — too close to the 3:1 non-text floor to keep as a shared token). Both now hardcode `#8a5a00` directly (`src/pages/index.astro`, `src/styles/global.css`) instead of referencing `var(--gold-ink)`, since the two contexts need different values and can't share one token. No visual or contrast change here — this is the same value the closing bands already had; only its literal-vs-token wiring changed.
- **No test changes required beyond the About width assertion above** — the only existing color assertions tied to gold-ink are the closing bands' dark-theme focus-ring checks (`rgb(138, 90, 0)`), which still resolve correctly since that spot was hardcoded rather than left on the redefined token. Verified via `eslint`/`astro check`/`tsc --noEmit` (all clean) plus manual visual verification in a running dev server; the full Playwright suite was not re-run for this narrowly-scoped follow-up.

## Content update: closing CTAs + About Training rewrite (2026-07-19, same day as above)

Two approved content changes, scoped narrowly per instruction — no design audit, no redesign of unrelated sections, no infrastructure changes. Left uncommitted per instruction.

- [x] **Home, About, and Expertise closing CTAs replaced with page-specific copy.** All three previously converged on attorney-access/sole-practitioner messaging (`Request a Consultation` / "Talk with the attorney who will actually handle your matter"; `Talk with Russ directly` / "Every matter is handled by the attorney you meet — not passed down the chain"; `Ready to Discuss Your Matter?` / "you'll work with the attorney who handles it"). Final copy (`src/pages/index.astro`, `src/pages/about.astro`, `src/pages/expertise.astro` — visual treatment, spacing, and light/dark closing-band behavior all untouched):
  - **Home** (`.close-section`): "Start with a conversation." / "Get clear advice on what comes next—and experienced representation when you need it." / button **Request a consultation**.
  - **About** (`.closing-band`): "Practical judgment. Proven experience." / "From day-to-day advising to trial and appeal, Russ brings both to every stage." / button **Talk with Russ**.
  - **Expertise** (`.closing-band`): "Let's find the right next step." / "Get clear guidance on your options—before a decision, during a dispute, or in court." (revised same day, tightened from an earlier longer draft) / button **Discuss your needs**.
  - All three still link to `/contact`; none reuse "matter" or "not passed down the chain."
- [x] **About — Training section rewritten and reformatted** (`src/pages/about.astro`, data moved out of the generic `expertise` array in `src/data/credentials.ts` into dedicated `trainingIntro` / `trainingAtAGlance` / `trainingExperience` exports, since Training's new shape no longer fits the plain `paragraphs`/`reportedDecisions` shape the other `expertise` entries use). Replaced six dense paragraphs with three parts:
  1. **Introduction** — one proof-led paragraph (approved copy verbatim), with `more than 150,000 participants` given `<strong>` emphasis via a small `splitHighlight()` helper in the page frontmatter (same pattern as the existing `splitDecision()` helper) rather than hardcoding markup into the data file.
  2. **Training at a glance** — three editorial columns (`Audiences`, `Employment topics`, `Governance and compliance topics`), each a heading plus a plain ruled `<ul>` (no pills/badges/icons), auto-fit down to one column on mobile.
  3. **Selected training experience** — the seven prior training-engagement entries, rewritten to the approved copy, as stacked archival rows (org name as label, description below, subtle `border-bottom` dividers, one quantity per row given selective `<strong>` emphasis — e.g. `more than 30`, `More than 100`, `Hundreds` — via the same highlight helper, driven by an optional `highlight` field per data entry).
  - **Width system**: the `#training` section itself dropped its old blanket `.about-narrow` (it now needs full editorial width for the three-column glance grid — same as `#credentials`); the intro `<p>` stays narrow automatically via the existing sitewide `.about-section p { max-width: var(--content-narrow) }` rule, and `.about-training-experience` opts back into `.about-narrow` explicitly (the same "wide section, narrow subsection" pattern already used for Affiliations inside Clients & Affiliations). No new width tokens introduced.
  - **Facts preserved verbatim**: 150,000+ participants; 5–10 to 1,000+ session sizes; English and Spanish; 30+ Council on Education in Management programs (1993–2002); 25+ Littler Mendelson presentations; 100+ Head Start sessions across 50+ centers; 100+ FQHC sessions across dozens of California clinics; 100+ nonprofit/service-organization programs; hundreds of client sessions. No organizations, totals, dates, or credentials invented.
  - **The revised Training copy remains subject to Russ's final factual review** — not yet confirmed by him.
- **Tests**: `tests/polish-pass.spec.ts` updated — the Home/About/Expertise CTA heading and button-label assertions now expect the new copy (button labels differ per page, so the two closing-band loop tests now map path → expected label instead of asserting one shared "Get in touch" string); the About width test now asserts `#training` is wide while its intro paragraph and `.about-training-experience` are narrow. One new consolidated test added (`Home, About, and Expertise closing CTAs carry page-specific copy, and About Training is rewritten with a glance summary and selected engagements`) covering the supporting-copy text on all three bands and the new Training structure (glance headings, 7 selected-experience rows). Per instruction, only this file was run (Desktop Chrome project, `tests/polish-pass.spec.ts`, temporary port to avoid the owner's own dev server on 4321) — 19/19 passed; the full suite, unit tests, and dependency checks were not re-run.
- **Verification performed**: `astro check`, `astro build`, and `eslint` all clean. Focused Playwright-driven visual review at 1280×800 and 390×844 (light theme only, per instruction) on Home/About/Expertise only: closing-band heading/copy/button wrapping and spacing, About's full Training section (intro, three-column glance grid, selected-experience rows), and every closing-band-to-footer transition — no overflow, clipping, awkward wrapping, or footer collision found on either page.
- **Still open**: Russ's factual sign-off on the rewritten Training copy (organizations, quantities, dates as listed above).

## Cross-browser hardening, build-time prefixing, and a Lighthouse pass (2026-07-21)

Three-part pass triggered by an explicit ask to stop relying on browser defaults, verify vendor prefixing at build time, and do a general pre-launch cleanup sweep. Left uncommitted per instruction.

- [x] **Explicit base reset added** (`src/styles/global.css`, right after the existing `* { box-sizing: border-box }`). Covers every element type actually used sitewide (checked via `grep` against every `.astro` file, not assumed): `ul`/`ol` (margin/padding/list-style), `figure`/`dl`/`dd` (margin), `address` (font-style), `strong` (font-weight), `svg` (display/max-width), `button`/`input`/`textarea` (font/color/margin/padding/border/background/appearance), and `summary` (cursor + native-marker suppression). Existing component-scoped rules (`.btn`, `.contact-form input`, `.theme-toggle`, etc.) already had their own explicit values and continue to win on specificity — the base reset only removes reliance on user-agent defaults for markup that had none.
- [x] **`<details>/<summary>` marker made explicit and identical across browsers.** The native disclosure marker differs in shape/position between Chrome/Firefox (`::marker`) and Safari (`::-webkit-details-marker`). Both are now hidden globally, replaced with one hand-drawn CSS chevron (border-based, rotates on `[open]`) added to the two actual instances — About's and Expertise's mobile jump-nav (`about.astro`, `expertise.astro`). Verified pixel-identical across Chromium, Firefox, and WebKit via a real three-engine Playwright screenshot comparison (not just Chromium DevTools emulation).
- [x] **Build-time vendor prefixing** — no PostCSS/autoprefixer/browserslist config existed before this pass; Astro/Vite's default CSS pipeline does not autoprefix on its own. Added `postcss.config.mjs` (autoprefixer) + `.browserslistrc` (`defaults`) + `postcss`/`autoprefixer` devDependencies. Confirmed working by inspecting `dist/_astro/*.css` after a real build: `-moz-columns`/`-moz-column-gap`/`-moz-column-break-inside` (the `.multi-column-list` `columns` shorthand), `-moz-placeholder`, `-webkit-text-decoration-skip` all now generated automatically. (`-webkit-appearance` is correctly *not* added — the `defaults` target's oldest Safari, 18.5, has supported unprefixed `appearance` since 15.4, so autoprefixer omits it rather than padding the CSS with a dead prefix.)
- [x] **Verification**: `astro check` (0/0/0), `eslint .` (clean), full `npx playwright test` — 73/73 passing (Desktop Chrome + Mobile Safari, 1 pre-existing unrelated WebKit skip) on a real production build, not dev server.
- [x] **Unused `public/` assets identified and moved to `original-images/`** — cross-referenced every file in `public/` against real usage in `src/`, `astro.config.mjs`, `netlify.toml`, `scripts/`, and `public/manifest.json` (not docs mentions, which had gone stale). `full-logo-white.webp` (superseded when the OG/Twitter generator switched to `footer-logo-white.webp` in an earlier pass, but the old file was never deleted), `av-preeminent.png`, and `super-lawyers-badge.png` (both award-badge graphics that were never actually rendered anywhere — the AV Preeminent/Super Lawyers proof points are styled text, not images) all had zero references. A stray `.DS_Store` that was silently shipping into every production build (Astro copies `public/` verbatim; `.gitignore` doesn't affect that copy) was deleted too — harmless in practice since real deploys build from git, where it was never tracked.
  - **Caution for future passes**: moving files into `original-images/` with `mv` silently overwrote two pre-existing files there with the same names — that directory is `.gitignore`'d, so there was no git history to fall back on, and this machine has neither Time Machine backups nor APFS local snapshots. No actual loss in this case (confirmed recoverable from older commits on `main` predating the Astro rebuild), but future moves into `original-images/` should check for a name collision first (`ls` before `mv`, or use `mv -n`).
- [x] **Lighthouse pass** (`npx lighthouse`, desktop preset on all 5 routes + mobile preset on Home) run against a real production build via `astro preview`, not dev server. Before this pass: Home/About/Expertise/Contact/Payment were already 100/100/100/100 on desktop; Home on mobile was 94/100/100/100 performance, driven by the hero portrait and header/footer brand marks being served at far larger resolution than they're ever displayed at (`square-profile.webp` used at up to 833px source for a 220–360px display box; `footer-logo-white.webp` at 2568px source for a 230px display box; `logo-white.webp` at 417px source for a 46px display box).
  - Generated right-sized WebP variants via `cwebp` (no new asset pipeline/dependency — matches the site's existing plain-`public/`-image convention rather than introducing `astro:assets`): `square-profile-440.webp` / `square-profile-640.webp` (Home hero now uses a real `srcset`/`sizes` matching the `.hero__portrait` grid's actual 220–320px range; About's fixed 180px portrait swapped straight to the 440w variant), `footer-logo-white-470.webp` (Footer.astro only), `logo-white.webp` replaced in place at a smaller resolution (Header.astro's only consumer — confirmed via grep before replacing).
  - **`public/footer-logo-white.webp` (the original, full 2568px-wide file) was deliberately left untouched** — `scripts/generate-social-images.mjs` composites it at up to ~56% of a 2400px OG-image canvas (~1344px), so it still needs full resolution; only Footer.astro's own small on-page usage was repointed to the new `-470` variant. Verified by re-running the generator against the untouched source (output byte-diffed as expected re-render noise against the currently-committed OG/Twitter PNGs, then reverted with `git checkout` since regenerating those wasn't itself in scope this pass).
  - Result: Home mobile performance 94 → **99/100**, LCP 3.0s → 1.7s, `uses-responsive-images` audit now fully passing. The one remaining minor flag (~10 KiB on the footer logo) is Lighthouse comparing a deliberately-2x-retina asset against a 1x CSS box — not a real issue, left as-is rather than shipping a blurry logo on retina displays.
  - `tests/polish-pass.spec.ts`'s footer brand-mark `src` assertion updated to match the new filename.
- **Files changed**: `src/styles/global.css`, `src/pages/about.astro`, `src/pages/expertise.astro`, `src/pages/index.astro`, `src/components/Footer.astro`, `postcss.config.mjs` (new), `.browserslistrc` (new), `package.json`/`package-lock.json` (postcss, autoprefixer), `tests/polish-pass.spec.ts`, `public/` (asset moves + new resized variants).

## Chevron follow-up, contact-form spam protection, security headers, and Privacy Policy (2026-07-21, same day as above)

Final polish pass of the launch-prep sweep, in two parts: (1) closing out the jump-nav chevron from the section above, which turned out to still have a load-time flash even after the position:relative fix; (2) a second round of pre-launch hardening (contact-form abuse protection, security headers, a Privacy Policy page) plus an OG/Twitter-card check. Left uncommitted per instruction.

- [x] **Jump-nav chevron rebuilt as a real inline `<svg>`, superseding the border-hack version described above.** The `::after` border-corner chevron (fixed 8×8 box, `translateY`/`rotate` combo) had a brief but visible "snap into position" flash on a hard page reload — traced (via `page.addInitScript()` + `requestAnimationFrame` sampling across the first ~1000ms) to not be a transition/computed-style-change issue at all; the actual root cause was relying on a manually-sized pseudo-element box that needed a hand-tuned pixel offset to visually center in the first place. Replaced in both `about.astro` and `expertise.astro`: `<summary>` is now `display: flex; align-items: center; justify-content: space-between`, with a real `<svg class="jump-nav-chevron" width="16" height="16" viewBox="0 0 12 12">` (a simple stroked chevron path) as a flex child — `align-items: center` centers it natively, no manual offset needed anywhere. Rotation on `[open]` (`transform: rotate(180deg)`) is unchanged in spirit, just applied to the SVG element instead of a pseudo-element. All the old `::after`/border/`translateY`/`position:relative;top` rules were removed entirely.
- [x] **Contact-form spam/abuse protection** (`netlify/functions/contact.ts`, `src/components/ContactForm.astro`, `src/styles/global.css`, `netlify.toml`) — the endpoint had shape validation (Zod) but nothing stopping a bot from hitting it directly. Added:
  1. A honeypot field (`name="company"`) — visually off-screen (`position: absolute; left: -9999px`, not `display:none`, which some bots specifically check for and skip), `tabindex="-1"`, and `aria-hidden="true"` on its wrapper, so real visitors (sighted or on assistive tech) never encounter it — verified by keyboard-Tab test that focus skips straight from the message field to Submit. If the server sees it filled, the submission is silently reported as `{ ok: true }` without calling Resend, so a bot gets no signal it was caught.
  2. Netlify-native rate limiting on `/api/contact` (`[redirects.rate_limit]`, 3 requests per IP per 180-second window — the max window Netlify allows).
- [x] **Site-wide security headers** (`netlify.toml`, new `[[headers]]` block for `/*`): `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (camera/microphone/geolocation/payment/usb all disabled — none used anywhere on the site), and a `Content-Security-Policy` (`default-src 'self'`, explicit `'none'`/`'self'` on every other directive, `frame-ancestors 'none'`). **One real tradeoff, not hidden**: `script-src`/`style-src` both need `'unsafe-inline'` because this is a static Astro build with no per-request server to hand out a nonce, and Astro inlines a handful of small scripts (theme toggle, mobile-menu toggle) and per-page stylesheets directly into the HTML. Verified before writing the CSP that the site uses zero external scripts/styles/iframes anywhere (`grep`-audited), so the meaningful protection — blocking any *remote* injected script/style/frame/object source — is intact; only inline-content restriction is weakened. A stricter hash-based CSP is possible later but would need a build-time hash-generation step, since inline content differs per page and per build.
- [x] **New `/privacy` page** (`src/pages/privacy.astro`) — plain page (not a checkbox/modal; that pattern is for GDPR-style marketing-consent flows, not needed here), linked from the footer (next to the copyright line) and from the contact-form disclaimer. Content reflects what the site actually does: only data collected is the contact form fields, sent via Resend, no analytics/tracking cookies, theme preference stored in `localStorage` only. **Not yet reviewed by Russ** — same as other legal/factual copy on the site, this should get his sign-off before being considered final.
- [x] **`robots.txt`** — added `Allow: /privacy`, matching the existing per-page-listed convention.
- [x] **OG/Twitter card — checked what's checkable without a login, rest handed to the owner.** Both Facebook's Sharing Debugger and X's Card Validator now require a logged-in session and can't be driven headlessly. Verified instead: `og:image` (2400×1262, ~1.9:1, matches Facebook's recommended ratio, 1.2MB well under its 8MB cap) and `twitter:image` (1010×506, ~2:1, matches the `summary_large_image` spec), both served as `image/png` over HTTPS with correct headers; all `og:`/`twitter:` meta tags in `BaseLayout.astro` are well-formed. The owner should still paste the live URL into both tools themselves to see the literal rendered card.
- **Tests**: new unit test in `tests/unit/contact.test.ts` (`a filled honeypot field is silently dropped as spam`) — asserts 200/`{ok:true}` and that the Resend client is never called.
- **Verification performed**: `astro check` (0/0/0), `eslint .` (clean), `node --test tests/unit/**/*.test.ts` via Node 24/nvm (12/12 passing, up from 11), `astro build` (privacy page included in output and sitemap), `netlify.toml` parsed with a TOML parser to confirm syntax, full `npx playwright test` (73 passed, 1 pre-existing unrelated WebKit skip — same count as before, no regressions), manual Playwright-MCP visual check of `/privacy` and the contact form, and a `browser_evaluate` + keyboard-Tab check confirming the honeypot field is genuinely unreachable by a real user.
- **Files changed**: `src/pages/privacy.astro` (new), `src/components/Footer.astro`, `src/components/ContactForm.astro`, `src/styles/global.css`, `netlify/functions/contact.ts`, `netlify.toml`, `public/robots.txt`, `tests/unit/contact.test.ts`, `src/pages/about.astro`, `src/pages/expertise.astro` (chevron only).

- [x] **OG/Twitter card — confirmed picked up correctly.** Both tools required a logged-in session and couldn't be driven headlessly (see above). Jared has a Facebook account, so he ran the live URL through **Facebook's own Sharing Debugger directly** — picked up correctly. He doesn't have an X/Twitter account, so X's card was instead checked via a third-party previewer (`camelai.com/tools/metadata-card-previewer`), which also picked it up correctly — not literally X's own crawler, but it parses the same `twitter:` tags and renders from them.

### Remaining project tasks (not completed this pass)

- Privacy Policy content — needs Russ's review/sign-off before final.
- Everything else tracked as "pending" in earlier sections of this doc (production cutover, final legal review, etc.) was already resolved per the Pre-launch TODO checklist above — nothing new was reopened by this pass.
